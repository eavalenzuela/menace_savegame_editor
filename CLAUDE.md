# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A savegame editor for the game MENACE. Python project (GPLv3 license). Currently in early development with no source code yet — only a sample save file (`main.save`) exists.

## Language & Tooling

- Python (`.gitignore` is Python-template based)
- No build system, package manager, or test framework configured yet

## Save File

`main.save` is a binary save file from the game MENACE, included as a reference for reverse-engineering the save format.
